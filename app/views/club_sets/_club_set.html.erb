<li>
  <h3><%= club_set.name %></h3>
  <% club_set.clubs.each do |club| %>
    <h4><%= "#{club.generic_club.club_type} : #{club.generic_club.brand}" %></h4>
    <%= link_to 'delete', supplier_club_set_path(supplier, club_set), method: :delete %>
  <% end %>
</li>
