<button class="create_form_btn" >Create Listing</button>

<%= form_for obj.listings.build, html: {id: 'new-listing-form', class: 'hide-form appending_listing'}, remote: true do |f| %>
  <%= f.hidden_field :user_id, value: @supplier.user_id %>
  <%= f.hidden_field :listable_id, value: obj.id %>
  <%= f.hidden_field :listable_type, value: obj.class.to_s %>
  <%= f.label :start_date %>
  <%= f.date_field :start_date, value: Date.current %><br>
  <%= f.label :finish_date %>
  <%= f.date_field :finish_date %><br>
  <%= f.label :price %>
  <%= f.text_field :price %><br>
  <%= f.submit %> 
  <%= f.submit "Cancel", type: "button", class: "btn-cancel" %>
<% end %>